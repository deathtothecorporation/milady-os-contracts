pragma solidity 0.8.18;

uint constant NUM_MILADYS_MINTED = 3; // 30 takes quite a while when forking from mainnet

address constant MILADY_AUTHORITY_ADDRESS = address(uint160(1808100010810));

address payable constant PROJECT_REVENUE_RECIPIENT = payable(address(uint160(1808100010811)));